# Custom PID Blocks

Place newly created custom P&ID symbol DXF files here.

The generator at `tools/pid/generate_attached_pid.py` can import these blocks
by name when referenced from `tools/pid/attached_pid_layout.json`.
